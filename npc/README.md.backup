# my_cpu

## 此项目为 ysyx_24080018 个人搭建的 CPU。

```bash
make config // 设置 debug 时参数
make run    // 编译
make wave   // 打开波形文件(gtkwave)
```

### LOG
**8.31**： 移植了 LightSSS，目前开启后支持停止后生成报错位置附近 1000 条指令大小的波形文件

**10.4**： 
1. 修改为多周期架构，将 IDU 与 EXU 模块放在同一个周期内完成，因此 auipc 这类指令需要三个周期（IFU -> IDU & EXU -> WBU)
2. 处理控制冒险: jal 指令导致的控制冒险需要花费额外的3个周期去取得正确的pc
